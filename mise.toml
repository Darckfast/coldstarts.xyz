[tools]
bun = "1.3.14"
wrangler = { version = "4.99", allow_builds = ["esbuild", "sharp", "workerd"] }

[tasks.init]
run = """
wrangler d1 migrations apply --local EDGE_COLD_STARTS_DB --persist-to .
"""

[tasks.dev]
run = "bun dev"
